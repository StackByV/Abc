import React from 'react';

const PageContainer = ({ title, children }) => {
  return (
    <div className="min-h-screen" style={{ backgroundColor: 'var(--color-bg-base)' }}>
      {/* Page Title Header */}
      <div 
        className="px-6 py-1.5"
        style={{ 
          backgroundColor: 'var(--color-bg-elevated)',
          borderBottom: '1px solid var(--color-border-primary)',
          color: 'var(--color-text-primary)'
        }}
      >
        <h1 className="text-base font-medium">
          {title}
        </h1>
      </div>

      {/* Page Content */}
      <div className="px-6 py-4">
        {children}
      </div>
    </div>
  );
};

// Example usage with the performance dashboard
const DashboardExample = () => {
  return (
    <PageContainer title="Unified Performance Dashboard">
      {/* Your content goes here */}
      <div 
        className="rounded-lg p-6 mb-4"
        style={{ 
          backgroundColor: 'var(--color-bg-elevated)',
          color: 'var(--color-text-primary)'
        }}
      >
        <h2 className="text-lg font-semibold mb-4">Dashboard Content</h2>
        <p className="mb-4" style={{ color: 'var(--color-text-secondary)' }}>
          This is your universal page container using your custom theme system. Place any content here including:
        </p>
        <ul className="list-disc list-inside space-y-2 mb-4" style={{ color: 'var(--color-text-secondary)' }}>
          <li>Metric cards and buttons</li>
          <li>Data grids (ag-grid)</li>
          <li>Charts and visualizations</li>
          <li>Forms and controls</li>
        </ul>
        
        <div className="text-sm" style={{ color: 'var(--color-text-tertiary)' }}>
          Theme automatically syncs with your ThemeProvider context
        </div>
      </div>

      {/* Example: Grid Container */}
      <div 
        className="rounded-lg p-4"
        style={{ backgroundColor: 'var(--color-bg-elevated)' }}
      >
        <div 
          className="h-64 border-2 border-dashed rounded flex items-center justify-center"
          style={{ borderColor: 'var(--color-border-primary)' }}
        >
          <div className="text-center" style={{ color: 'var(--color-text-tertiary)' }}>
            <div className="text-lg font-medium mb-2">AG Grid Goes Here</div>
            <div className="text-sm">Replace this with your ag-grid component</div>
          </div>
        </div>
      </div>
    </PageContainer>
  );
};

export default DashboardExample;
